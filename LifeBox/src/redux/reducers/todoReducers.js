const INITIAL_STATE = {
    todos: []
};

const todosReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state
    }
};

export default {
    todos: todosReducer,
}